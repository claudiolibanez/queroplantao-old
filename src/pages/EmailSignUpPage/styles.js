import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  titleContainer: {
    marginHorizontal: 24,
    marginTop: 16,
    marginBottom: 24,
  },

  formContainer: {
    marginHorizontal: 24,
    paddingBottom: 24
  },
  input: {
    marginBottom: 24
  },
})

export default styles